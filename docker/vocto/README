# VocTo Docker Container

This container provided [VocTo](http://asimeon.de/vocto/) ([@github](https://github.com/simeonackermann/VocTo)) in a container.

## Usage instructions

Build the container:

	docker build -t vocto

Run it:

	docker run -p 8081:80 vocto
